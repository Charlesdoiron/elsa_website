import React from 'react';
import { connect } from 'react-redux';

import PropTypes from 'prop-types';

const Home = props => {
	return (
		<div>
			<p>Home</p>
		</div>
	);
};

Home.propTypes = {};

export default Home;
